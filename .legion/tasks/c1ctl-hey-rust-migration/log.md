# Log: C1ctl Hey Rust Migration

- User clarified that `axiomctl` is the wrong durable name; the Rust CLI should be `c1ctl`.
- User confirmed the migration should be staged: rename to `c1ctl`, establish the Rust CLI architecture, migrate the first core non-Rofi `hey` command slice, and leave the rest for follow-up PRs.
- Created isolated worktree `.worktrees/c1ctl-hey-rust-migration` on branch `legion/c1ctl-hey-rust-migration-core` from `origin/master` at `37d72934`.
- Wrote research, RFC, and implementation plan. RFC chooses a staged foundation-first migration: `c1ctl` canonical naming plus Rust `path`/`which`/`help`/dynamic resolver compatibility, while high-impact mutating commands remain delegated to Janet `hey` for this PR.
- RFC review failed on two blocking issues: ambiguous `@rofi` boundary and missing child-process environment contract for Rust-executed dynamic scripts. Updated RFC to delegate `@rofi` whole to Janet `hey` and to require `DOTFILES_HOME`, computed `PATH`, `HEYSCRIPT`, `HEYDRYRUN`, and `HEYDEBUG` for Rust-executed scripts.
- Re-ran RFC review after fixes; decision PASS. Accepted RFC for implementation.
- Implemented the approved first slice: moved the Rust package to `packages/c1ctl`, renamed binary/package/env names, updated Axiom host wiring and README, kept mode/status/reload behavior, added Rust `path`/`which`/`help`/dynamic resolver/exec foundation, delegated mutating commands and `@rofi` to Janet `hey`, and set the specified script execution environment for Rust-executed dynamic scripts.
- Local engineer checks passed: `rustfmt` completed with the known non-fatal Nix eval-cache warning, `nix build .#c1ctl` passed, help works without `DOTFILES_HOME`, and safe behavior checks passed for path/which/help, `@rofi` delegation, delegated `help sync`, and active `HEYDRYRUN`/`HEYDEBUG` env propagation through `c1ctl exec env`.
- Formal verification PASS recorded in `docs/test-report.md`: rustfmt check, package build, CLI behavior/env/delegation checks, Axiom package eval, Axiom toplevel dry-run, current-code stale reference check, and `git diff --check`.
- Implementation review failed on namespace bypass, stale wiki truth, and incomplete security-sensitive validation evidence. Fixed namespace parsing so only `[A-Za-z0-9_-]` namespaces are accepted, `@rofi` remains an exact delegated boundary, and `@@rofi` / path traversal forms fail. Enhanced validation to assert computed `PATH`, exact `HEYSCRIPT`, delegated Janet dry-run/debug logs, and negative Rofi bypass checks.
- Updated Legion wiki current truth: `c1ctl` supersedes `axiomctl`, the old `axiomctl-cli-consolidation` summary is historical, current decisions/patterns/maintenance describe staged non-Rofi Rust migration and exact `@rofi` delegation. Re-ran validation after fixes and wiki updates; all checks passed.
- Second implementation review found non-Rofi dynamic resolver traversal could still reach `config/rofi/**`. Fixed dynamic segment validation for all base resolvers and added a final `config/rofi/**` resolved-path guard. Added negative validation for namespace, `wm`, and dot-command traversal forms.
- Final implementation review PASS with security lens applied. Residual risks are limited to live post-deploy Axiom mode/reload smoke and future delegated command-family migrations.
- Prepared walkthrough artifacts: `docs/report-walkthrough.html`, `docs/report-walkthrough.md`, and `docs/pr-body.md`. `pr-html-render` handoff is artifact-only/local render by explicit scope decision; no GitHub Pages preview workflow was added.
- Opened PR https://github.com/Thrimbda/dotfiles/pull/105 from branch `legion/c1ctl-hey-rust-migration-core`.
