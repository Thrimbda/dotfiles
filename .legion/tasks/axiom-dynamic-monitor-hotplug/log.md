# Log

- 2026-06-17: Task opened from live Axiom display incident. User confirmed dynamic mode policy should keep native/highest resolution and choose the highest refresh at that resolution. Upstream Caelestia docs confirm per-monitor overrides live under `~/.config/caelestia/monitors/<screen-name>/shell.json` and override global `shell.json` only for supported options.
- 2026-06-17: RFC initially failed review because unknown-output behavior and hotplug trigger were underspecified. RFC was updated to make Axiom unknown outputs use native-max-refresh with auto positioning and to add a Hyprland event socket watcher. Re-review passed.
- 2026-06-17: Implementation completed in `.worktrees/axiom-dynamic-monitor-hotplug` on branch `legion/axiom-dynamic-monitor-hotplug-monitor-policy`. Axiom now defines known monitor identity/policy in `modules.desktop.hyprland.monitors`, enables unknown-output native-max-refresh hotplug, generates a reconcile helper, and installs a Hyprland-session event watcher. Verification so far: generated `monitors.conf` eval, monitor inventory eval, Home Manager activation build, NixOS toplevel build, `bash -n` for generated helper/watcher, jq sample confirming unknown 4K120 selects 4K120 instead of 1440p240, and `git diff --check`.
- 2026-06-17: Change review found and fixed a Caelestia seed helper temp-file cleanup issue. Re-ran monitor config eval, Home Manager activation build, NixOS toplevel build, and `git diff --check`; all passed. Review verdict is PASS.
